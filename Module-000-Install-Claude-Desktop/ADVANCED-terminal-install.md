# Advanced: the command-line install

This course does not need it. If you would rather run Claude from a command line, the April 2026 package covers that route in full: the installer, the PATH fixes and the Windows PowerShell steps.

Ask Danny for the SSL 26 Edition package, or see the archived copy in the curriculum repository under `.Archives/2026-04-terminal-edition/`. Both editions write the same files in the same places, so you can switch between them at any point.
